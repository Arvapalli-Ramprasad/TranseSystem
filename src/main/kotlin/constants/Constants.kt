package org.example.constants

object Constants {
    // MongoDB Configuration Constants
    const val MONGO_DB_HOST = "mongo.host"
    const val MONGO_DB_PORT = "mongo.port"
    const val MONGO_DB_NAME = "mongo.database"

    // Server Configuration Constants
    const val SERVER_HOST = "server.host"
    const val SERVER_PORT = "server.port"

    // Kafka Configuration Constants
    const val KAFKA_BOOTSTRAP_SERVERS = "kafka.bootstrap.servers"
    const val KAFKA_USERNAME = "kafka.username"
    const val KAFKA_PASSWORD = "kafka.password"
    const val KAFKA_TOPIC = "kafka.topic"
}
